<!DOCTYPE html>
<html lang="es">
<head>
    <title>Cita Médica</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .header {
            text-align: center;
            margin-bottom: 50px;
        }
        .section {
            margin-bottom: 30px;
        }
        .section .title {
            font-weight: bold;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
<div class="header">
    <h1>Cita Médica</h1>
</div>

<div class="section">
    <div class="title">Datos del Paciente:</div>
    <p>Nombre: {{cita.datos_paciente.nombre}} {{cita.datos_paciente.primer_apellido}} {{cita.datos_paciente.segundo_apellido}}</p>
</div>

<div class="section">
    <div class="title">Datos del Especialista:</div>
    <p>Nombre: {{cita.datos_especialista.nombre}} {{cita.datos_especialista.primer_apellido}} {{cita.datos_especialista.segundo_apellido}}</p>
    <p>Especialidad: {{cita.datos_especialista.especialidad_nombre}}</p>
</div>

<div class="section">
    <div class="title">Datos de la Cita:</div>
    <p>Fecha: {{cita.datos_cita.fecha}}</p>
    <p>Hora: {{cita.datos_cita.hora}}</p>
    <p>Consulta: {{cita.datos_cita.consulta_nombre}}</p>
</div>

<div class="section">
    <div class="title">Código QR:</div>
    <img src="{{qr}}" alt="Código QR">
</div>
</body>
</html>